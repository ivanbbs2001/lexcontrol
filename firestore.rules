rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow read/write on the configured workspace
    match /workspaces/{workspaceId}/data/{document} {
      allow read, write: if true; // Troque por autenticação quando necessário
    }
  }
}
